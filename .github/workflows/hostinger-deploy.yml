name: 🚀 Auto Deploy to Hostinger (AIVANA ToDoList)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔑 Deploy via Secure FTPS to Hostinger
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.todolist.barkataiautomation.in
          username: u786522790.todolistaiautomation
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./
          server-dir: /domains/todolist.barkataiautomation.in/public_html/
          log-level: verbose
          security: strict
          exclude: |
            **/.git*
            **/.github*
            **/.vscode*
            **/node_modules/*
